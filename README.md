# faticai
   ```bash
   git init
   git add .
   git commit -m "Initial commit - Sito collocamento Napoli"
   git branch -M main
   git remote add origin https://github.com/tuo-username/tuo-repository.git
   git push -u origin main
